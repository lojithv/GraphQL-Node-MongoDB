#Graphql Yoga - Error Masking

**Reference:** https://the-guild.dev/graphql/yoga-server/docs/features/error-masking

##Install dependencies

To install dependencies `use yarn`, `yarn install`, or `npm install` etc.

##Run Command

`node yoga.js`